Module src.utils.Accumulator
============================

Classes
-------

`Accumulator(n)`
:   For accumulating sums over `n` variables.
    
    Args:
        n: number of variables

    ### Methods

    `add(self, *args)`
    :   Adds the arguments passed.

    `reset(self)`
    :   Resets the cummulative data.